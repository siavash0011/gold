<Window x:Class="GoldShop.Pages.ClientEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="افزودن مشتری" Height="270" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name Field -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="نام" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid>
                <TextBox x:Name="NameBox" Height="30" VerticalContentAlignment="Center"/>
                <TextBlock Text="مثلاً: علی رضایی"
                           IsHitTestVisible="False"
                           Foreground="Gray"
                           Margin="5,0,0,0"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Text, ElementName=NameBox}" Value=""/>
                                        <Condition Binding="{Binding IsKeyboardFocused, ElementName=NameBox}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </StackPanel>

        <!-- Phone Field -->
        <StackPanel Grid.Row="1" Margin="0,15,0,0">
            <TextBlock Text="تلفن" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid>
                <TextBox x:Name="PhoneBox" Height="30" VerticalContentAlignment="Center"/>
                <TextBlock Text="مثلاً: 09123456789"
                           IsHitTestVisible="False"
                           Foreground="Gray"
                           Margin="5,0,0,0"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Text, ElementName=PhoneBox}" Value=""/>
                                        <Condition Binding="{Binding IsKeyboardFocused, ElementName=PhoneBox}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
            <Button Content="ذخیره" Width="80" Height="30" Click="Save_Click"/>
            <Button Content="انصراف" Width="80" Height="30" Margin="10,0,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>